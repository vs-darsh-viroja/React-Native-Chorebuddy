import React, { useState } from 'react';
import { Image, Pressable, StyleSheet, Text, View } from 'react-native'; import * as Notifications from 'expo-notifications'; import { Images } from '@/constants/assets'; import { colors, font, isPad, s } from '@/theme';
export function NotificationPermissionView({ onFinish }: { onFinish(): void }) { const [requesting, setRequesting] = useState(false); const enable = async () => { setRequesting(true); await Notifications.requestPermissionsAsync(); setRequesting(false); onFinish(); }; return <View style={styles.root}><Image source={Images.appBg} resizeMode="cover" style={StyleSheet.absoluteFill} /><Text style={styles.title}>Never Forget Your Chores Again</Text><View style={styles.lines}><Text style={styles.line}>Get notified when you don't complete your chores on time.</Text><Text style={styles.line}>Chore reminders are optional and can be configured individually.</Text><Text style={styles.line}>We don't use notifications for marketing.</Text></View><Image source={isPad ? Images.ipadReminderImg : Images.iphoneReminerImg} resizeMode="contain" style={styles.reminder} /><View style={styles.flex} /><View style={styles.actions}><Pressable disabled={requesting} onPress={() => { void enable(); }} style={styles.primary}><Text style={styles.primaryText}>Enable Notification</Text></Pressable><Pressable disabled={requesting} onPress={onFinish} style={styles.secondary}><Text style={styles.secondaryText}>No Thanks</Text></Pressable></View></View>; }
const styles = StyleSheet.create({ root: { flex: 1, backgroundColor: colors.background, alignItems: 'center', paddingTop: s(55), paddingHorizontal: s(25), paddingBottom: s(40) }, title: { ...font('semibold', 34), color: colors.text, width: s(280), textAlign: 'center' }, lines: { width: s(320), gap: s(15), marginTop: s(45) }, line: { ...font('regular', 18), color: colors.text }, reminder: { width: isPad ? s(500) : s(333), height: s(260), marginTop: s(34) }, flex: { flex: 1 }, actions: { width: '100%', gap: s(15) }, primary: { height: s(52), borderRadius: s(26), backgroundColor: colors.purple, alignItems: 'center', justifyContent: 'center' }, primaryText: { ...font('semibold', 16), color: 'white' }, secondary: { height: s(52), borderRadius: s(26), borderWidth: 1, borderColor: colors.purple, alignItems: 'center', justifyContent: 'center' }, secondaryText: { ...font('semibold', 16), color: colors.text } });
